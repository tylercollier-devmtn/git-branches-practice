function add(a, b) {
  // This is a comment!
  const num1 = a;
  const num2 = b;
  return num1 + num2;
}

function sub(a, b) {
  return a - b;
}

function mult(a, b, c) {
  // Harder math!
  return a * b * c;
}
